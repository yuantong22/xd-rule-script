# JOURNAL · xd-rule-script 开发日志

> 项目：脚本规则工作台（xd-rule-script）
> AI 助手：Qoder（作业模板中的 .claude/CC 在本项目对应 Qoder + .qoder/rules/）

## 日志结构

- **每日流水账 + 当天的认知变化**：见 [`Day-by-day/`](Day-by-day/day1.md) 文件夹（day1 ~ day7）
  - 流水账（做了什么 / 卡在哪 / 怎么解开的）由 AI 助手在开发过程中实时记录素材
  - "对 AI 编程助手的认知变化"每天一小节，由本人撰写
- **一周维度的总结**（下面 4 节）：跨天沉淀，开发满一周后回顾撰写

| 天 | 文件 | 主题 | 状态 |
|---|---|---|---|
| Day 1 | [day1.md](Day-by-day/day1.md) | 注册 Google 邮箱 + 上车 CC，3 小时被封后转投 Qoder | 已记录 |
| Day 2 | [day2.md](Day-by-day/day2.md) | 需求定稿 + 文档拆分 + 仓库上云 | 已记录 |
| Day 3 | [day3.md](Day-by-day/day3.md) | 拿到百炼 Key，重做实施计划（22 个任务）+ 深夜开工搭骨架 | 已记录 |
| Day 4 | [day4.md](Day-by-day/day4.md) | 核心功能冲刺：占位符三件套 → 沙箱运行 → 真 AI 审查 | 已记录 |
| Day 5 | [day5.md](Day-by-day/day5.md) | 验收返工日：5 个体验优化 + AI 幻觉修复 + 全能型视角觉醒 | 已记录 |
| Day 6 | [day6.md](Day-by-day/day6.md) | 录屏验收日：qwen3.8-max 端点不兼容翻车、改 qwen3-max，悟到选模型要够强+兼容+真验证 | 已记录 |
| Day 7 | [day7.md](Day-by-day/day7.md) | 复盘日：计划书 598 KB 的真相 + 一周回顾 | 已记录 |

---

## 一、"原来还能这样"（一周内至少两件）

**总纲：我以前是「点对点」，真正的 AI coding 是「按范式输出」。**

以前的工作方式是一事一议：想到哪说到哪、口头交代一句、AI 答完这轮就散了，下一轮还得从头说。这一周体会到的是另一套 —— 每个环节都按范式走，**每一步都留下磁盘上的产出物，下一步直接接着用**，事半功倍。四个例子：

1. **需求不是我写文档丢过去，是 AI 逐条问我答**（Day 2）
   - 走 brainstorming：把「只支持 Groovy」「占位符 `${变量名}`」「校验必须同步等大模型返回」「一条规则一个会话」「接口全 POST」这些决策一条条敲定，然后 AI 成文，还把我那份混在一起的 PRD 拆成《需求文档》+《技术方案》两份
   - 收益：这两份文档被后面 22 个任务反复引用，`.qoder/rules/` 里直接写「需求以它为准」—— **一次敲定，全程复用**

2. **UI 风格不是「做个好看的」，是三选一**（Day 2）
   - 一次性出三套**可交互**的 HTML 风格稿（浅色商务 / 深色代码风 / 现代渐变），本地起预览服务对比着挑，选定「风格三」
   - 收益：主色 `#5b5fc7 → #8b5cf6`、大圆角、柔和阴影写进 rules，后面所有前端任务照这个基准做，**界面风格一次返工都没有**。点对点的做法是「不好看，再改改」，能来回改十轮

3. **计划不是一句「帮我实现」，是 22 个任务 + 全局约束**（Day 3）
   - 关键动作是把 TDD「先红后绿」提升成**全局约束**，而不是留在某个任务的局部说明里 —— 因为计划要并行分发给子代理，局部说明会被割裂，拿到任务 6 的子代理根本看不到任务 13 的解释
   - 更重要的是：**范式化的东西才可审计**。粗读一遍就查出三件 AI 没主动说的事（任务 8～22 只有摘要、明文 API Key 躺在文档里、假 TDD）；把 Step 顺序摊开对比，又发现任务 2、3 是「实现写在测试前面」的假 TDD，任务 6 被误判成「薄封装不用测」，覆盖率数字当天从 15/22 修正到 18/22
   - 这些缺陷单个任务看都完全合规，**只有摊开在全局层才看得见**

4. **规矩不是每次交代，是写进 rules 每轮自动生效**（Day 2～3）
   - `.qoder/rules/` 两份文件（接口规范 + 开发约定），每轮对话自动注入，不花上下文预算
   - 收益：不用每次重复说「接口全 POST」「错误信息必须中文」「仅支持 Groovy」

**反面对照，恰好证明了这件事**：Day 5 那 5 个体验返工（没返回按钮、运行按钮太远、变量名被省略号截断、刷新丢填的值、没校验就能点保存），**恰恰是没走范式的部分** —— 需求文档里只写了前后端交互，没有交互动线设计，22 项验收清单一条都没覆盖到。走范式的部分（界面风格、TDD、接口规范）零返工，没走范式的部分全靠返工补。

一句话：**点对点靠记性和返工，范式化靠产出物复用。**

## 二、"这玩意还不行"（一周内至少一件）

1. **计划书生下来就 598 KB，把执行方的上下文撑爆**
   - 14,064 行里 73% 是完整实现代码（java 5,446 行、vue 1,995 行、js 1,665 行、bash 539 行）
   - 病根是 `writing-plans` 技能的 No Placeholders 规则明文要求「每个代码步骤必须贴真实代码」「不许写参照任务 N，要重复贴一遍」
   - 后果：执行时时不时超出上下文被压缩，压缩后连「做到哪了」都要靠猜。更糟的是这些代码在任务完成那一刻就全部过期 —— **等于抄了两遍，还留下一份必然失真的副本**

2. **它不会维护自己造的追踪机制**
   - 计划书里 201 个勾选框，七天里一次没勾过（只有 10 个勾，全在手工验收清单里）；真正当账本的一直是 git log
   - 第 219 行「后 15 个待展开」这句过期信息，从开工到收尾没人改，还提交进了 git
   - **一份失真的账本比没有账本更危险**

3. **多会话并发时，互相是瞎子**
   - 9/3 两个会话同时在跑，一个以为做到任务 17，另一个正在写任务 19 的文件，双方完全看不见对方；那个计划文件 3 分钟内长了 14 行
   - 而 `docs/superpowers/plans/` 当时还没纳入 git，连 diff 和回滚都做不到

4. **同一个坑会踩两次**
   - Maven 那个死掉的内网 Nexus 镜像，Day 3 踩过、Day 6 又踩一遍 —— 知识只留在当天的对话里，不会自动沉淀成 rules，得靠人提醒

5. **环境 + 网络 + 鉴权交织的问题，只能一步步试探**
   - GitHub 推送报 `Error in the HTTP2 framing layer`，实际是本机只放行 `api.github.com` 和 `codeload.github.com`、主站不通，配 Clash 代理才过；后来前台跑 `git push` 又会被 IDE 的 Node fallback 静默打断，改后台才成
   - `/usr/bin/git` 是坏空壳（CommandLineTools 自 2019 年残缺），Homebrew 也坏了，只能手动装整套工具链
   - 8080 端口反复「already in use」但 lsof 又查不到：`kill ... 2>/dev/null` 把报错吞了，得出「已释放」的假结论；重查才发现一整套**跑了一天多的旧 JVM** 还占着端口，我改的环境变量根本没生效（只在进程启动时读一次）

6. **自然语言指代会误读，而且错了还一路错下去**
   - Day 5「这个非常不好看，还有小数点，展示全了」，我连续两次理解成返回按钮的样式问题，返工两版；实际说的是**填值表格里变量名被省略号截断**（「小数点」= 省略号）
   - 直到第三句「为什么这么简单的问题搞了很多次」才反应过来

7. **工具报错和 AI 自己算的数字，都不能尽信**
   - Write / SearchReplace 报 unknown 大多是误报（文件其实写好了），但有一次是真的 0 字节 —— 盲目重试会把好文件写坏
   - Day 7 我自己写的 awk 统计脚本算出「java 代码块 38 万行」（整个文件才 1.4 万行），差点写进结论
   - 9/3 先断言「做到任务 17」，两分钟后被 git 里新冒出的任务 18 提交打脸

8. **会幻觉，而且幻觉得很自信**
   - 主动建议脚本里写 `context.put(...)`，但沙箱 binding 里只有占位符，根本没有 context 对象
   - 根因是提示词没交代运行时环境里有什么；反过来在 CR 提示词里写明「`${age}` 是占位符语法，不是非法代码」，验收时真调大模型就是零噪音

9. **模型能力就是天花板，调 prompt 只是逼近它；但「最新」不等于「能用」**
   - 同一套代码、同一套 prompt，`qwen-plus` 的审查和对话明显偏「降智」，换 `qwen3-max` 直接上一个台阶
   - 反面：最新的 `qwen3.8-max` 只在百炼兼容模式端点放出，而项目 spring-ai-alibaba 走原生端点，一换上去 CR 直接 `HTTP 400 url error`、AI 全挂；更坑的是当时只 curl 了兼容端点 + 看 health UP 就以为成了（假阳性，health 和 AiService 初始化都不发真实模型请求），等业务里真调一次 AI 才暴露。选模型要「够强 + 跟端点/SDK 兼容 + 真验证过」三者齐全

## 三、再给一周，我会怎么改进

### 方面一（产品侧）：给工作台里的 AI 加 FAQ 知识库 + RAG 长期记忆

指的是**交付给用户的产品功能** —— `ChatPanel` 那个 AI 对话助手，和 `AiService` 的 CR 审查。

**现状**（查过代码，不是印象）：

- 记忆机制是 `MessageWindowChatMemory` + `InMemoryChatMemoryRepository`，`maxMessages(40)`；真实历史存在 MySQL `message` 表，首次访问或重启后由 `ChatMemoryService.ensureLoaded()` 从库里捞最近 `WINDOW_SIZE = 40` 条回灌
- 产品知识**全是硬编码在提示词里的**：`CR_SYSTEM_PROMPT` 里那两段【重要背景】（`${变量名}` 是占位符不是语法错误）和【运行时环境】（沙箱 binding 只注入占位符，不存在 context / log / out / session / request）

**四个缺口**：

1. **窗口外等于失忆** —— 超过 40 条（约 20 轮）的历史再也捞不回来。代码注释自己写着「再大会让每次请求的 token 明显上涨」—— 也就是说现在是**被 token 成本逼着牺牲记忆**
2. **检索能力为零** —— 只能「按时间捞最近 N 条」，不能「按相关性捞」。用户三周前提过的偏好，就算还在库里也捞不出来
3. **知识只能靠加长提示词** —— Day 5 修 `context.put(...)` 幻觉的办法就是往提示词里再加一段。这条路的天花板很明显：知识越多提示词越长，而**每轮都要全量发送，不管用户问的是什么**
4. **跨会话不共享** —— 一条规则一个会话，换个规则就什么都不记得

**要做的三块**：

1. **FAQ 知识库** —— 把需求文档、技术方案、交互规则 #1～#9、沙箱黑名单清单、占位符语法与五种类型规则切块向量化。用户提问先检索 FAQ，命中就带着出处回答。**这直接替代现在硬编码在提示词里的那两段**，而且装得下多得多的知识
2. **长期记忆 RAG 化** —— 窗口内保留最近几轮原文（保对话连贯），窗口外改成向量检索按需捞（保长期）。MySQL 那份完整历史不动，它负责「可回放」；RAG 只负责「按相关性捞」
3. **检索注入链路** —— query → 同时检索 FAQ 与长期记忆 → 拼进 prompt → 流式返回，答案带出处引用

**为什么这件事在这个项目特别值**：Day 5 那次 AI 幻觉的根因就是「AI 不知道运行环境里有什么」，当时的解法是往提示词里硬写一段。知识少的时候硬写没问题，知识一多就必须换成检索 —— 这个项目已经撞上这个拐点了。

### 方面二（协作侧）：给 Qoder 外挂知识库 + 长期记忆存 RAG

指的是**我自己的工作方式** —— 让编程助手跨会话不失忆。

**为什么要做**（这周暴露的痛点）：

- 同一个坑踩两次（Maven 死镜像 Day 3 / Day 6）—— 知识留在当天的对话里，压缩一次就没了
- `.qoder/rules/` 只能装少量硬规矩，装不下长尾经验（环境细节、历史决策的理由、踩坑记录）
- 流水账是给人读的叙事，不是给机器检索的
- **多会话互相是瞎子** —— 9/3 两个会话同时写计划书谁也不知道，今天 16:52 又一笔我没做的提交落进来；两次都是事后核对 git 才发现

**先划清分工边界**（这是前提，不然什么都往里塞）：

| 层 | 装什么 | 特点 |
|---|---|---|
| `.qoder/rules/` | 每次都必须遵守的硬约束 | 每轮自动注入、不花上下文预算，**量必须小** |
| RAG 知识库 | 长尾经验：踩坑记录、环境细节、历史决策及理由、FAQ | 按需检索，可以很大 |
| 长期记忆 | 用户偏好、项目事实 | 跨会话生效 |
| 流水账 / JOURNAL | 人读的叙事与心得 | 给人看，不承担检索职责 |

**和方面一的分工别混**：方面一是**产品特性**，跑在 `script_back` 里，要进需求文档和验收清单；方面二是**工具配置**，跑在 IDE 侧，要进 rules 和工作流。两边技术栈可能都是 RAG，但归属完全不同。

### 两者共用：什么叫「做到生产级」

不管产品侧还是协作侧，达到这六条才算生产级，不然只是个 demo：

1. **写入有门槛** —— 一条知识必须能回答「什么场景会用到它」，答不上来就不收
2. **检索可量化** —— 建一套真实问题集测 top-k 命中率，不能靠「感觉搜到了」
3. **答案必须带出处** —— 给出是哪条知识、什么时候写的；没有出处的检索结果和幻觉没区别（这个项目被 `context.put` 幻觉咬过一次）
4. **有失效机制** —— Maven 镜像那条哪天网络修好了、沙箱黑名单哪天加了新条目，对应知识要能撤回或重新索引。**过期的知识比没有更危险** —— Day 6 的 `qwen3.8-max` 假阳性是同一个病：验的是过期路径
5. **冲突有优先级，写死** —— 产品侧：当前代码 > 需求文档 / 技术方案 > FAQ 向量库 > 大模型常识；协作侧：当前代码 > `.qoder/rules/` > RAG > 记忆
6. **从真实提问里长出来** —— 产品侧的 FAQ 从用户实际问过的问题提炼，协作侧的知识从流水账的「卡在哪」反向提炼，都不凭空编

### 方面三：新需求严格走 superpowers 的分层范式

按这周的教训把流程钉死（细节见 [day7.md](Day-by-day/day7.md)）：

1. **brainstorming** → 逐条问答敲定需求，产出《需求文档》+《技术方案》两份**分离**的文档
2. **交互设计前置** → 立案初期就出可交互原型（像 Day 2 那三套 UI 风格稿），把动线、状态、错误恢复先画出来，别等验收才发现「没返回按钮」
3. **writing-plans，但加项目级覆盖** → 五条：贴测试不贴实现 / 一次只展开 3～5 个任务 / 一份计划一个子系统 / 单份 ≤60 KB / 每个任务开头一张 5 行卡片
4. **分层执行** → 「驾驶舱」（进度实况 + 全局约束 + 任务总览，约 300 行）常驻，任务详情按需展开；**禁止整份读**；定位只用文本锚点，不记行号
5. **能并行就 subagent-driven** → 计划阶段就标明哪些任务无共享状态，并写明「用 subagent 并发派发」，否则标了也会退化成串行（这次就是）
6. **verification-before-completion 收尾** → 任何「我做完了」前面必须有一次真实命令输出，而且**验的必须是真路径** —— Day 6 只 curl 了兼容端点 + 看 health UP 就以为换模型成功了，可 health 和 `AiService` 初始化都不发真实模型请求，等业务里真调一次 AI 才暴露 400。**假阳性比失败更贵**
7. **需求文档当活文档** → 交互规则随做随补。Day 5 那三条（#7 返回导航 / #8 填值缓存 / #9 保存前置校验）补晚了，22 项验收清单因此漏掉 5 个体验问题，全部变成返工

**外加两条纪律**：

- **进度只认一个地方** —— 要么提交后同一轮内更新计划书，要么干脆别设勾选框，用 git log 当唯一账本
- **选外部依赖要三者齐全** —— 够强 + 跟端点/SDK 兼容 + 真验证过。「最新」不等于「能用」

### （以下两条是 AI 补充，可删）

- **方面四：环境类知识做一次性沉淀** —— 把这周踩的环境坑（`/usr/bin/git` 空壳、PATH 不含 `/usr/local/bin`、git 2.20.1 没有 `switch`/`restore`、Maven 死镜像要带 `-s`、8080 假死进程、`git push` 必须后台跑）一次性整理进 rules，止血
- **方面五：把「像新用户一样从头用一遍」定成验收固定动作** —— 清单只能验「想到的」，探索式使用才能验「没想到的」；Day 5 那 5 个问题一个都不在清单里

## 四、换个角色思考（可选，200~500 字）

> 场景：让一个完全没碰过 AI 编程助手的同事开始用它做本职工作，我会给他配什么 skill / agent / hook？为什么？

**我不设想「没碰过的同事」，我把自己当产品。**

痛点不是写不出代码，是**产出物别人看不懂**：MQ、CI/CD、QPS 满天飞，我写技术文档，读的是用户和老板。他们不会说「没看懂」，只会脑补一个意思再往下走。

配三样：

1. **术语翻译官** —— 主动把名词降维成白话，且要译「所以呢」：「MQ 积压」翻成「确认短信晚到 3 分钟，影响两千单」
2. **自然语言查数** —— 问「通过率为什么降」自动查数仓，但口径先确认、答案带出处，否则数字会骗人
3. **异动自动归因 @ 负责人** —— 按贡献度排序给原因；@ 人前先排除数据管道延迟，误报两次就没人信了

还有个区分：**skill 是「能做什么」，hook 是「必须做什么」**。技术人最容易把该做成 hook 的事做成 skill，指望自己记得用 —— 这周的 TDD 就是靠写进 rules 才活下来的。

一句话：**AI 时代技术人的稀缺能力，不是把事做出来，是把事讲到别人能决策。**
