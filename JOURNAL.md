# JOURNAL · xd-rule-script 开发日志

> 项目：脚本规则工作台（xd-rule-script）
> AI 助手：Qoder（作业模板中的 .claude/CC 在本项目对应 Qoder + .qoder/rules/）
> 说明：流水账部分由 AI 助手在开发过程中实时记录素材，感受类章节由本人撰写。

---

## 1. Day-by-day 流水账（卡在哪、怎么解开的）

### Day 1（2026-09-01）：需求定稿 + 文档拆分 + 仓库上云

**做了什么**
- 通过多轮问答把需求从零敲定：脚本只支持 Groovy、占位符用 `${变量名}`、校验必须同步等大模型审查返回、一条规则一个会话、接口全部 POST
- 产出《需求文档》《技术方案》两份文档，并做了三套 UI 风格稿，选定"风格三：现代渐变风"
- 初始化 git 仓库并推送到 GitHub

**卡住的地方**
- 推送 GitHub 一直报 `Error in the HTTP2 framing layer`：排查发现本机网络只放行 `api.github.com` 和 `codeload.github.com`，github.com 主站不通。最后配置 Clash 代理（127.0.0.1:7897）后推送成功，中间还有一次是纯网络抖动，重试即过
- 本机环境坑：`python3` 因缺 Xcode CommandLineTools 报错不可用；`node` 未安装（前端开发前需要补装），临时用系统自带的 ruby 起了静态预览服务

**怎么解开的**
- 网络问题：逐个通道探测（主站 / api / codeload / git 智能接口）定位到"主站被限"，再用代理解决，而不是盲目重试
- 环境问题：逐个 `which` 排查可用工具，找到替代路径

---

## 2. 对 AI 编程助手的认知变化（第 1 天 vs 第 7 天）

（待撰写：开发进行到第 7 天后，对照流水账回顾，写具体例子）

- 第 1 天的印象：
- 第 7 天的印象：
- 认知转变的具体例子：

---

## 3. "原来还能这样"（至少两件）

（候选素材，待挑选后展开撰写）

- [ ] 候选 1：一次性生成三套可交互的 UI 风格稿（HTML + 本地预览服务），像点菜一样挑界面
- [ ] 候选 2：需求文档和技术方案可以口述式逐条敲定，AI 负责成文、拆分、归档、提交
- （继续补充…）

---

## 4. "这玩意还不行"（至少一件）

（候选素材，待挑选后展开撰写，可附截图 / prompt 片段）

- [ ] 候选 1：GitHub 推送这类"环境 + 网络 + 鉴权"交织的问题，AI 也只能一步步试探，第一次还挂起干等了半天
- （继续补充…）

---

## 5. 再给一周，我会怎么改进

（待撰写）

---

## 6. 换个角色思考（可选，200~500 字）

> 场景：让一个完全没碰过 AI 编程助手的同事开始用它做本职工作，我会给他配什么 skill / agent / hook？为什么？

（待撰写）
